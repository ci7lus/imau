query queryLibrary($state: StatusState!, $after: String) {
  viewer {
    works(state: $state, after: $after) {
      nodes {
        id
        annictId
        malAnimeId
        title
        noEpisodes
        episodes {
          nodes {
            viewerDidTrack
          }
        }
      }
      pageInfo {
        hasNextPage
        hasPreviousPage
        endCursor
      }
    }
  }
}
